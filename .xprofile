#! /bin/sh

if [ -d /etc/X11/xinit/xinitrc.d ]; then
    for f in /etc/X11/xinit/xinitrc.d/*; do
        [ -x "$f" ] && . "$f"
    done
    unset f
fi

export DE="xfce"
export EDITOR="nvim"

xset -dpms &
xset s off &
setxkbmap -option compose:menu &

